spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/account
    username: account
    password: account
  jpa:
    show-sql: true
  liquibase:
    enabled: false

apps:
  base-url:
    currency: http://localhost:8090/currency